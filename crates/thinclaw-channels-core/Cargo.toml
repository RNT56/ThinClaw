[package]
name = "thinclaw-channels-core"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
thinclaw-identity = { path = "../thinclaw-identity" }
thinclaw-types = { path = "../thinclaw-types" }
chrono = { version = "0.4", features = ["serde"] }
futures = "0.3"
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde"] }
