# Legacy Persona Seed Stub

This legacy file is kept only for migration history.

- Old seed name: `default`
- Current personality pack: `balanced`
- Canonical source: `assets/personality_packs/balanced.md`

New code should load `agent.personality_pack` and the files under `assets/personality_packs/`.
