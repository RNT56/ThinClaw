//! Extra commands — now empty; commands moved to commands/gateway.rs.
//!
//! `openclaw_test_connection` and `openclaw_switch_to_profile` are registered
//! directly from `openclaw::commands::*` in lib.rs.
//!
//! This module is kept as a placeholder to avoid breaking the module tree.
//! Nothing is exported from here anymore.
